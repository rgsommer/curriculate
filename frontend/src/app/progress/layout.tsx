export const metadata = {
  title: "Student Progress | Curriculate",
  description: "View your grading results and track progress over time.",
};

export default function ProgressLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
