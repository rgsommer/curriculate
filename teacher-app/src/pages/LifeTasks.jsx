// This page moved to the public Next.js site at frontend/src/app/tasks/page.jsx
// (deployed to www.curriculate.net/tasks). The teacher-app version was removed
// because the teacher-app deploys to set.curriculate.net, not www.
//
// File kept (and emptied) instead of deleted because the working sandbox
// could not delete it. Safe to `rm` whenever convenient.
export default function LifeTasksMoved() {
  return null;
}
